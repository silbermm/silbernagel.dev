image: ubuntu/lts
packages:
  - elixir
  - libgpgme-dev
tasks:
  - setup: |
      mix deps.get 
      mix compile --warnings-as-errors 
  - run_analyzer: |
      mix dialyzer --plt
      mix dialyzer --halt-exit-status --format github
